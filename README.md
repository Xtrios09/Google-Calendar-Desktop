# Google Calendar Desktop

A lightweight Electron-based desktop wrapper that brings Google Calendar to a dedicated desktop app.  
Includes support for building a Windows `.exe` using `electron-builder`.

---

## ✨ Features
- Loads Google Calendar in a clean desktop window  
- Minimal footprint, fast startup  
- Buildable to a Windows installer or portable `.exe`  
- 100% local — no API keys or backend required  

---

## 📦 Installation

Clone the project and install dependencies:

```bash
npm install
```
---

Or just download it from the download section.
